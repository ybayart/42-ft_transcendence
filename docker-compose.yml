version: '2.0'
services:
  ruby:
    build: ./docker/ruby/
    ports:
      - "3000:3000"
    volumes:
      - ./srcs:/data
    environment:
      FT_TRANSCENDENCE_DATABASE_PASSWORD: password
    tty: true
    stdin_open: true
  postgresql:
    build: ./docker/postgresql
    environment:
      POSTGRES_PASSWORD: password
