<%= form_for(room_member, :as => "room_member", url: room_members_path, remote: true) do |form| %>
  <% if room_member.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(room_member.errors.count, "error") %> prohibited this room_member from being saved:</h2>

      <ul>
        <% room_member.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
	<%= form.collection_select :user_id, User.all - @room.members, :id, :nickname %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
