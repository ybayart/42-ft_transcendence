<%= form_for(guild_member, :as => "guild_member", url: guild_members_path, local: true) do |form| %>
	<% if guild_member.errors.any? %>
		<div id="error_explanation">
			<h2><%= pluralize(guild_member.errors.count, "error") %> prohibited this guild_member from being saved:</h2>

			<ul>
				<% guild_member.errors.full_messages.each do |message| %>
					<li><%= message %></li>
				<% end %>
			</ul>
		</div>
	<% end %>

	<div class="field">
		<%= form.label :user_id %>
				<%= form.collection_select :user_id, User.all - @guild.members, :id, :nickname %>
	</div>

	<div class="actions">
		<%= form.submit %>
	</div>
<% end %>
