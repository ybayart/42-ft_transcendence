<%= form_for(profile_friend, :as => "friendship", url: profile_friends_path, remote: true) do |form| %>
  <% if profile_friend.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(profile_friend.errors.count, "error") %> prohibited this profile_friend from being saved:</h2>

      <ul>
        <% profile_friend.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user %>
	<%= form.collection_select :friend_b_id, User.all - @profile.friends, :id, :nickname %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
